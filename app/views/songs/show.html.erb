<h1>Song Name: <%= @song.name %></h1>
Genre: <%= link_to @song.genre.name, genre_path(@song.genre) if @song.genre %><br>
Artist: <%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %>